[user]
    email = inneekim@gmail.com
    name = Inna Kim

[alias]
    br = branch
    cm = commit
    co = checkout
    df = diff
    l = log
    lg = log --decorate --oneline --graph --branches --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold blue)(%ar) %C(bold yellow)%d%C(reset) %C(green)%s%C(reset) %C(dim green)- %an%C(reset)'
    p = pull
    st = status

[credential]
    helper = cache --timeout=72000

[grep]
    lineNumber = true
[push]
    default = simple
