- clean: ['~']

- link:
   ~/.vimrc: vimrc
   ~/.tmux.conf: tmux.conf
   ~/.gitconfig: gitconfig
   ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - 
    command: ./programs.sh
    stdin: true
    stdout: true
    stderr: true
